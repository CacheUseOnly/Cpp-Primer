/* Explain what happens in each of the if tests
 * if (42 = i)   //  ... if (i = 42)   //  ...
 */

int main() {
    if (42 = i)   //  expression must be a modifiable lvalue
    
    if (i = 42)   //  (i = 42) returns value of i, which is 42, a non-zero value

    return 0;
}