/* In the binary search program on page 112, 
 * why did we write mid = beg + (end - beg) / 2; instead of mid = (beg + end) /2;?
 */

// (end -  beg) / 2 is the increment, need beg added here to make it the correct index